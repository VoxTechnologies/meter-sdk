# @meter-mcp/cli

Command-line interface for Meter prepaid credits and MCP usage billing.

## Install

```bash
npx @meter-mcp/cli
```

or install it globally:

```bash
npm i -g @meter-mcp/cli
```

## Commands

Command documentation lands in a later revision of this package.
