# Contributing

Open an issue before proposing a substantial public API change. Pull requests
must include tests, documentation, and a Changeset when they alter published
behavior.

```bash
npm install
npm run changeset
npm run verify
```

Do not submit Meter service credentials, internal server code, private API
contracts, customer data, or security reports. Use the private reporting
channel in SECURITY.md for vulnerabilities.
